/**
 * Interface that represents metadata related to control value adapter.
 */
export interface ControlValueAdapterMetadata {
  /**
   * Unique name for the control value adapter.
   */
  name: string;
}
